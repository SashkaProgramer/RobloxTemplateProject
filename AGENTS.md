# Project Agent Rules

- Before any project-specific actions, read all `Docs/**/*.md` files if you do not currently have reliable context from them (for example, in a first chat or after context compression).
- By default, do not read files under `Docs/ForUser/**`; this section is user-facing personal notes and should be ignored unless the user explicitly asks to use it.
